#include<stdio.h>
int main()
{
    int t,n;
    double fact=1;
    scanf("%d",&t);

    for(int j=1; j<=t; j++)
    {
        scanf("%d",&n);

        for(int i=1; i<=n; i++)
        {
            fact = fact*i;
        }
        printf("%0.0lf\n",fact);
        fact=1;
    }
}
